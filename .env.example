# WhatsApp AI Agent Environment Configuration

# User Identity & Timezone
USER_NAME="محمد"
TIMEZONE="Asia/Riyadh"

# AI Provider Selection: 'mock' (for offline testing) | 'ollama' (free local) | 'gemini' (free tier) | 'groq' (free tier)
AI_PROVIDER="mock"

# Optional Cloud API Keys (if not using local Ollama or mock mode)
GEMINI_API_KEY=""
GROQ_API_KEY=""

# Ollama Endpoint (if using local AI)
OLLAMA_HOST="http://127.0.0.1:11434"
OLLAMA_MODEL="llama3:latest"

# Voice Configuration
VOICE_ENABLED=false
TTS_VOICE_NAME="ar-SA-HamedNeural"

# WhatsApp Session Storage Path
SESSION_DATA_PATH="./baileys_auth_info"
